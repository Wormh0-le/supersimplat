# AI Select Tickets moved

The authoritative AI Select Ticket contracts now live at:

`docs/ai-select/tickets/`

The current planning manifest is:

`docs/ai-select/manifest.json`

This directory is a compatibility pointer only. Do not add new Ticket contracts here.
