# AI Select Domain Authority

Read for AI Select behavior, terminology, scope, or current authority.

Current authority is Final Spec Amendments 009→001, Final Spec v2.0 where not amended, current mapping, ADRs 0030→0020 where current, carried-over ADRs, context overlays 009→001, graph/review/tickets, then code/tests/runtime/benchmarks. Runtime remains implemented v1.3 until explicit cutover.

## Stable domain model

- Automatic acquisition is default; Expert Recovery retains Add Observation and Continue Acquisition.
- Seed is a prior; Core, seed-independent Envelope, reversible component Frontier, and Context are distinct.
- q/s Consensus is deterministic, bounded, regional, and non-executable.
- View Utility uses a finite layered pool and hybrid prospective probe; only the winner receives formal acquisition.
- Browser owns Acquisition Series/Attempt/Iteration state, Decision Journal, and deterministic budgets.
- Candidate publication uses two gates: exact Publication Eligibility plus Readiness/terminal/consent class.
- Only eligible Ready-low-gain auto-publishes; eligible forced Ready and Limited require explicit state-specific actions.
- Re-Lift recomputes and is not an alias for accepting an existing snapshot.
- A running Attempt may block Candidate application without making the prior Candidate stale.
- Candidate and Native Selection remain separate; Candidate never self-applies.

Context overlays are temporary and supersede conflicting root glossary terms until controlled consolidation.